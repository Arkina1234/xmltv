name: tvtv.us
on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  run_script:
    runs-on: ubuntu-latest

    steps:
    - name: checkout repository
      uses: actions/checkout@v4

    - name: Set up Node
      uses: actions/setup-node@v4
      with:
           node-version: 18

    - name: npm grab epg
      run: |
          rm -rf .git/
          git init
          git remote add origin https://github.com/Arkina1234/epg.git
          git status
          npm install
          npm run grab -- --channels=./channels/tvtv.us.channels.xml --gzip --output=./guide/tvtv.us.xml --days=3
          ls ./guide
          
    - name: Commit changes
      run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add -f -u guide/tvtv.us.xml
          git add -f -u guide/tvtv.us.xml.gz
          git commit -a -m "Update EPG"
          git push -f origin master
