import { DateTime as Luxon } from 'luxon';
export declare class DateTime {
    _dt: Luxon;
    constructor(value?: string | Date);
    utc(): Luxon;
    add(value: number | undefined, unit: string): Luxon;
    subtract(value: number | undefined, unit: string): Luxon;
}
