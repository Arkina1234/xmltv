"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DateTime = void 0;
const luxon_1 = require("luxon");
const units = {
    y: 'years',
    year: 'years',
    years: 'years',
    M: 'months',
    month: 'months',
    months: 'months',
    q: 'quarters',
    quarter: 'quarters',
    quarters: 'quarters',
    w: 'weeks',
    week: 'weeks',
    weeks: 'weeks',
    d: 'days',
    day: 'days',
    days: 'days',
    m: 'minutes',
    minute: 'minutes',
    minutes: 'minutes',
    h: 'hours',
    hour: 'hours',
    hours: 'hours',
    s: 'seconds',
    second: 'seconds',
    seconds: 'seconds'
};
class DateTime {
    constructor(value) {
        if (value instanceof Date) {
            this._dt = luxon_1.DateTime.fromJSDate(value);
        }
        else if (value === undefined) {
            this._dt = luxon_1.DateTime.now();
        }
        else {
            this._dt = luxon_1.DateTime.fromISO(value);
        }
    }
    utc() {
        return this._dt.toUTC();
    }
    add(value = 0, unit) {
        const key = units[unit] || 'days';
        return this._dt.plus({ [key]: value });
    }
    subtract(value = 0, unit) {
        const key = units[unit] || 'days';
        return this._dt.minus({ [key]: value });
    }
}
exports.DateTime = DateTime;
