import { DateTime as Luxon, DateTimeOptions } from 'luxon';
export declare class DateTime {
    _dt: Luxon;
    constructor(value?: string | Date | Luxon, options?: DateTimeOptions);
    utc(): DateTime;
    toString(): string;
    toJSON(): string | null;
    toJSDate(): Date;
    add(value: number | undefined, unit: string): DateTime;
    subtract(value: number | undefined, unit: string): DateTime;
    format(format: string): string;
}
